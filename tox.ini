[tox]
envlist = py-{pylint,flake8,isort,rstlint,pydiatra,pydocstyle,safety,pytest,black,bandit,yamllint}
skipsdist = True

[testenv]
basepython = python3.10
setenv =
    PYTHONPATH = src
deps = -r{toxinidir}/requirements-dev.txt
sitepackages = false
envdir =
    py: {toxworkdir}/py
allowlist_externals =
    make
    echo
commands =
    requirements: echo "Install requirements"
    flake8: flake8 src
    pylint: pylint src
    pydiatra: make pydiatra-script
    isort: isort --check-only --diff src
    bandit: bandit -r src --exclude src/{env:APP}/tests
    pydocstyle: pydocstyle --match=".*\.py" src --count
    safety: safety check
    pytest: py.test --cov-report xml --cov-report term-missing --cov src --nomigrations -vv
    rstlint: rst-lint README.rst doc.rst
    black: black . --check
    yamllint: yamllint deployment .github
