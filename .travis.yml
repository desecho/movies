language: python

python:
  - "3.5"

after_success:
  - codecov

branches:
  only:
    - master

env:
  - TOXENV=pylint
  - TOXENV=flake8
  - TOXENV=isort
  - TOXENV=py35

before_install:
  pip install -r requirements-dev.txt

script:
  - tox
