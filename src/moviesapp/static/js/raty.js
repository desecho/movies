// Generated by CoffeeScript 1.7.1
var ratySettings;

ratySettings = {
  path: '/static/bower/raty/lib/images/',
  cancel: true
};

if (language == 'ru') {
  $.extend(ratySettings, {
    hints: ['Ужасно', 'Плохо', 'Нормально', 'Хорошо', 'Отлично'],
    cancelHint: 'Отменить эту оценку',
    noRatedMsg: 'Пока нет оценки',
  });
}

$(function() {
  var score_settings, settings;
  score_settings = {
    score: function() {
      return $(this).attr('data-rating');
    }
  };
  settings = $.extend({}, ratySettings, ratyCustomSettings, score_settings);
  return $('.rating').raty(settings);
});
